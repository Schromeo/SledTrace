# SledTrace v0.7.1 — Trustworthy Local Tracing

SledTrace v0.7.1 is a backward-compatible reliability release for the existing
local-first Python SDK, Go Collector, SQLite store, and React Dashboard. It
focuses on making recorded evidence and local startup behavior more honest and
predictable. It also includes observed LLM usage visibility, one bounded
caller-instrumented Python tool path, and an explicit OpenAI Responses usage
helper with indicative text-token pricing. It does not broaden the product
into a hosted platform or a general agent runtime.

## Highlights

### Trustworthy span timing

- Added public `SpanTiming` and `t.measure()` for measuring the actual retrieval
  or LLM operation with UTC boundaries and monotonic elapsed time.
- Preserved explicit retrieval `duration_ms` and LLM `latency_ms` inputs.
- Unmeasured post-hoc spans now remain unmeasured instead of reporting false
  near-zero durations.
- Dashboard timing treats unknown, measured zero, and legacy fallback values as
  distinct states and no longer substitutes a partial span sum for trace time.

### Retrieval score semantics

- Normalized chunks retain `score_type` and `score_direction` when known.
- Similarity/relevance scores remain higher-is-better; distance is explicitly
  lower-is-better; ambiguous tuple scores remain unknown.
- The Collector applies low-score thresholds and score ordering only where
  higher-is-better semantics are established. It does not invent a
  `1 - distance` conversion.
- The Dashboard labels score direction as `Similarity ↑`, `Distance ↓`, or
  unknown where appropriate.

### Observable trace delivery policy

- Existing synchronous `flush()` behavior remains strict and exception-raising.
- New `try_flush()` performs one synchronous delivery attempt and returns
  `TraceFlushResult(ok, response, error)` for ordinary failures.
- The original delivery exception remains inspectable, while callers can keep a
  telemetry failure from replacing successful business work or an existing
  application exception.
- No retry, queue, disk buffer, background worker, automatic logging, or
  auto-flush behavior was introduced.

### Safer local network defaults

- Native Collector, Vite development, and Vite preview listeners default to
  `127.0.0.1`.
- Docker Compose publishes Collector and Dashboard host ports on loopback by
  default while preserving container-internal listeners.
- Collector CORS defaults to the exact local Dashboard origins instead of `*`.
- Intentional remote access remains available through explicit binding, allowed
  origin, Dashboard API URL, and SDK Collector URL configuration.

### Observed execution and compatibility

- The Dashboard presents LLM input/output/total tokens per observed call, with
  a known subtotal, coverage, and explicit missing, invalid, or conflicting
  values. Calls without an explicit provider source remain unverified.
- A synchronous Python `tool()` record captures caller-supplied safe summaries,
  status/error, timing, and span ID. Failed LLM attempts may retain supplied
  usage; `log_task_result()` records the final task outcome separately from
  intermediate model responses. The SDK does not run an agent or tool.
- The source-only Federalist Papers exercise uses local SQLite FTS5 retrieval
  and an explicitly simulated extractive answer; it is not a framework adapter
  or a real provider call.
- Historical warning rows with text `confidence="heuristic"` now read as
  unknown instead of making trace detail return HTTP 500. Stored rows are not
  rewritten, and numeric confidence remains readable.

### Explicit Responses usage and indicative cost

- Optional `sledtrace.openai.record_response` reads the model and usage from
  one completed, non-streaming OpenAI Python SDK Responses result supplied by
  the application. It does not make a provider call or import the OpenAI SDK.
- The helper stores no prompt, output text, response ID, or credentials. Token
  totals, cached input, cache-write and reasoning subfields retain their
  provenance; subset fields are never added twice.
- The Dashboard labels that explicit source per call and estimates Standard
  **text-token-only** USD cost for exact `gpt-4.1-mini` and `gpt-4o-mini`
  model/snapshot IDs using rates checked on 2026-09-24. Missing, invalid or
  conflicting usage, unsupported models, and nonzero cache-write counts are
  unpriced rather than treated as zero.
- The estimate is not a bill. Tool charges, alternate tiers, regional uplifts,
  hidden retries and uninstrumented calls are outside the observed scope.
  E3 was validated with sanitized offline fixtures and local readback, not a
  paid provider run or billing reconciliation.

## Install

```bash
python -m pip install sledtrace==0.7.1
sledtrace version
```

The wheel contains the SDK and CLI. `sledtrace serve` still requires a SledTrace
source checkout; Collector and Dashboard runtime assets are not bundled in the
Python wheel.

## Compatibility

- Preferred Python import: `sledtrace`
- Temporary legacy import: `raglens`
- Preferred Collector URL variable: `SLEDTRACE_COLLECTOR_URL`
- Temporary legacy Collector URL fallback: `RAGLENS_COLLECTOR_URL`
- Implemented span types are `retrieval`, `llm`, and one synchronous `tool`
- OpenAI Responses recording is an explicit optional helper, not automatic
  capture; existing caller-supplied usage and legacy imports remain supported
- Collector API routes and SQLite schema remain compatible
- Strict `flush()` calls keep their existing return and failure behavior

Remote users who previously relied on all-interface defaults must now configure
the address/host and browser origins explicitly. Local SDK and command-line
requests without an `Origin` header continue normally.

## Historical reliability-candidate validation

The earlier reliability-only candidate passed the checks below. They do not
validate the expanded exact-main candidate after E1/E2/X1/X2:

- 62 Python SDK tests
- 0.7.1 wheel and sdist build plus Twine metadata validation
- clean-wheel preferred/legacy imports, CLI, timing, score, and delivery checks
- all Go Collector tests
- 10 Dashboard behavior tests and the production build
- default and intentional-remote Docker Compose configuration expansion
- nine live SDK → Collector → SQLite → Dashboard reference traces
- three refreshed 1440x950 screenshots captured from the real Dashboard
- clean clone of release-prep commit `25521d4`: locked Dashboard install, new
  Python venv, installed CLI version/startup, health and Dashboard HTTP, conflict
  trace round trip, browser detail, and clean Git status
- protected PR #3 checks: Python 3.9, Python 3.13, Go Collector, and Dashboard

Historical candidate checks above are not proof of the final E3-inclusive
artifact. The release gate is a clean final-tree build and metadata check,
protected PR merge, immutable matching tag, protected PyPI publication,
production-index clean install, and GitHub Release.

## Exact-main candidate validation (2026-09-24)

From post-X2 `main` (`92b27b9`), before release-candidate documentation commit:

- The complete `release` validation profile passed all nine steps with normal
  Windows process permissions and an explicit isolated pytest temporary
  directory (forward-slash path). SDK: 68 passed; startup supervision: 18 passed.
- Wheel/sdist built as 0.7.1; Twine 7.0.0 accepted both 0.7.1 artifacts.
- Clean-wheel validation passed preferred/legacy imports, CLI, tool contract,
  timing, score semantics, and delivery policy. A copied independent app
  exercised success, application error, and Collector-offline paths.
- All Go packages passed; Dashboard 27 tests and production build passed.
- An isolated local Collector/Dashboard at ports 4327/5177 stored an offline
  `tool-recovery` trace in temporary SQLite. Browser inspection showed two
  observed LLM calls, 24 known tokens, tool failure then recovery, preserved
  error detail, and explicit unverified usage provenance.

The default Windows sandbox denied process cleanup, pytest's default temporary
directory, and Vite config access. Initial aggregate attempts were interrupted;
the final aggregate run passed with normal permissions and an isolated temporary
directory. A fresh checkout of candidate commit `cb0dfd0` completed locked
`npm ci`, real startup/health, SDK ingestion, warning readback, CORS, shutdown,
port release, and clean Git status. PR #11's five required CI checks passed.

`npm audit` reported four transitive Dashboard toolchain advisories (one
moderate, three high) under Babel/Browserslist and Vite/PostCSS. Their fixable
versions should be reviewed before publication; no dependency upgrade or
production exploitability conclusion is part of this candidate documentation.

Candidate visual review captured three conversation screenshots from the real
Dashboard using the sanitized offline tool-recovery trace: trace header,
usage/ordered steps, and the selected tool-error detail. Labels, error text,
unknown usage provenance, and the no-warning caveat were legible with no
secrets or private data visible. The later E3-inclusive review added a
full-width, sanitized [Responses usage screenshot](../assets/screenshots/openai-responses-usage.jpg)
to the root README. It shows a local offline fixture, not a paid provider call.

Docker runtime smoke remains dependent on a host with working Docker
virtualization. Static Compose validation alone is not evidence that containers
started successfully.

## Final E3-inclusive candidate acceptance (2026-09-25)

- PRs #11, #12 and #13 merged in order under protected `main` after each
  layer's required checks passed. Final product baseline before this release
  documentation/lockfile closure was `7bcbeec`.
- The nine-step release profile passed against the combined tree: 77 SDK tests,
  18 startup tests, wheel/sdist build, clean-wheel and copied independent-app
  validators, all Go Collector packages, 33 Dashboard tests and production
  build, plus diff hygiene. Its initial attempt used a pytest temporary path
  inside the source checkout and was stopped when an out-of-checkout CLI test
  entered the source serve path; rerunning with an isolated external temp path
  passed all steps.
- The optional OpenAI helper is present in the built wheel. A sanitized
  non-streaming response was stored through a local Collector and shown in the
  real Dashboard with input 120, output 80, total 200, cached input 20,
  coverage 1/1, and `$0.000170 USD` indicative text-token cost. No OpenAI
  request or billing comparison was made.
- Four reported transitive Dashboard dependency advisories were fixable
  within the existing version ranges. The release closure updates only the
  lockfile. An isolated clean checkout ran `npm ci`, audited zero findings,
  passed all nine release-profile steps and had clean Git status. Release PR
  #14 then passed all five required checks and merged as `33d2335`. Annotated
  tag `v0.7.1` points to that commit. The protected tag workflow built both
  distributions, passed Twine check and published them to production PyPI on
  2026-09-25. A clean virtual environment outside the repository installed
  the published wheel, verified preferred/legacy/OpenAI imports, CLI version
  and help, and the expected exit-1 guidance from `sledtrace serve` outside a
  source checkout. The matching GitHub Release is published. No paid provider
  call or bill reconciliation was performed.

## Not included

v0.7.1 does not add:

- authentication, TLS, cloud hosting, or multi-tenancy
- agent, memory, retry, or streaming spans beyond the one synchronous `tool`
- LangChain or LlamaIndex adapters
- LLM-as-judge or calibrated factuality scores
- delivery retries or atomic trace/warning persistence
- a standalone Collector/Dashboard runtime inside the Python wheel
